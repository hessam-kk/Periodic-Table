#pragma once
#include "Elements.hpp"

int search_by_name(char *, Element *);
void to_lowercase(char *);

int search_by_elementNumber(const int, Element *);

int search_by_symbol(char *, Element *);

float search_by_weight(const float, Element *);
